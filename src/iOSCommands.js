const cmd = require("node-cmd");

console.log("First");
async function getBundleId() {
    // cmd.run('osascript -e \'id of app " + appPath + "\'');
    console.log("First");
    
    // await cmd.run('node -v')
}
module.exports = getBundleId;
